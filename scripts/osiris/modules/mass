alias massinv {
  if ([$0]) {
    ^on ^who * {
       if (([$1] != [$N])&&([$0] != [$C])) {^timer $rand(30) ^invite $1}
       }
    echo $G inviting $(cyn)$0$(cl) to $(hwht)$C
    ^who $0-
    wait
    ^on who - *
  }{
    ^ohelp massinv
 }
}
